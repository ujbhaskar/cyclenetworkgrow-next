import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminContentPage() {
  return (
    <PlaceholderPage
      title="Content"
      description="Quotes, testimonials, products, partners — see docs/REQUIREMENTS.md §3.7."
    />
  );
}
