import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminDocumentsPage() {
  return (
    <PlaceholderPage
      title="Document Library"
      description="Unlinked + event-linked documents — see docs/REQUIREMENTS.md §3.8."
    />
  );
}
