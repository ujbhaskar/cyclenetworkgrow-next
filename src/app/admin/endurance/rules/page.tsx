import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminEnduranceRulesPage() {
  return (
    <PlaceholderPage
      title="Endurance Program — Rules"
      description="Medal/points rule sets per category — see docs/REQUIREMENTS.md §3.9."
    />
  );
}
