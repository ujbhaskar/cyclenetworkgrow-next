import PlaceholderPage from "@/components/PlaceholderPage";

export default function AdminStravaSubscriptionPage() {
  return (
    <PlaceholderPage
      title="Strava Subscription"
      description="View webhook subscription status, manual create/delete fallback — see docs/ARCHITECTURE.md §5."
    />
  );
}
