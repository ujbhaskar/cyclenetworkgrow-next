import PlaceholderPage from "@/components/PlaceholderPage";

export default function ConnectStravaPage() {
  return (
    <PlaceholderPage
      title="Connect Strava"
      description="OAuth connect/disconnect flow — see docs/ARCHITECTURE.md §5."
    />
  );
}
